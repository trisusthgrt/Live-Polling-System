{
  "name": "live-polling-system",
  "version": "1.0.0",
  "description": "A real-time polling system with teacher and student interfaces",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix backend/frontend",
    "dev": "npm run dev --prefix backend",
    "start": "npm start --prefix backend",
    "build": "npm install && npm install --prefix backend && npm install --prefix backend/frontend && npm run build --prefix backend",
    "dev:backend": "npm run dev:backend --prefix backend",
    "dev:frontend": "npm run dev:frontend --prefix backend"
  },
  "keywords": [
    "polling",
    "real-time",
    "education",
    "react",
    "nodejs",
    "socket.io",
    "mongodb"
  ],
  "author": "Sushobhan Tripathy",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
